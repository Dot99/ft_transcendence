services:
  backend:
    build: .
    ports:
      - "3000:3000"
    env_file: .env
    volumes:
      - .:/app
      - /app/node_modules
      - ./db/data.db:/db/data.db
    networks:
      - ft_transcendence-network

networks:
  ft_transcendence-network:
    driver: bridge
